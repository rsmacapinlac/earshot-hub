"""earshot — the Raspberry Pi conversation recorder.

Run as ``python -m earshot``. Hardware lives behind the HAL (see ``earshot.hal``);
the whole application runs off-device against the stub HAL.
"""

__version__ = "1.3.4.1"
